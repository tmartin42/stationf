'use strict';

describe('myApp.bookingHistory module', function() {

  beforeEach(module('myApp.bookingHistory'));

  describe('bookingHistory controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bookingHistoryCtrl = $controller('BookingHistoryCtrl');
      expect(bookingHistoryCtrl).toBeDefined();
    }));

  });
});